# SVGA Parsing

本上下文定义 SVGA 解析能力在性能优先与可用性保障之间的公共边界，使调用方能够准确理解解析策略及其降级语义。

## Language

**解析后端**:
负责把 SVGA 数据转换为可播放数据的实现；当前包括 WASM 后端和 JS 后端。
_Avoid_: Parser 类型、执行线程

**JS 执行通道**:
JS 解析后端实际运行的位置；可以是 Worker，也可以是主线程。
_Avoid_: JS 解析器、解析后端

**自动解析策略**:
优先选择高性能解析路径，并在可用路径失败时逐级降级，以尽量完成解析；其可用性保障包含从 WASM 后端降级到 JS 后端，以及从 Worker 通道降级到主线程通道。
_Avoid_: WASM 自动降级、Worker 模式

**执行通道失败**:
JS 解析任务因 Worker 构造、启动、通信或意外终止而无法完成；自动解析策略可以把同一任务转移到主线程通道。
_Avoid_: 解析失败、资源错误

**解析失败**:
解析通道正常工作，但 SVGA 下载、格式或数据解码无法完成；更换执行通道不应自动重试这类失败。
_Avoid_: Worker 失败、通道错误

**解析可用性底线**:
当资源本身可解析且环境能够执行主线程 JS 时，WASM 后端和 Worker 通道同时不可用不应阻止解析完成；性能路径失败只能触发可观察的降级。
_Avoid_: 零配置集成、静默容错

**开发集成诊断**:
开发工具配置导致解析后端或执行通道无法按设计加载时，面向开发者提供的可操作配置提示；它不属于 Player 运行时日志，也不改变解析结果。
_Avoid_: 运行时错误、解析失败

**解析器释放**:
取消当前解析会话并释放其执行通道，不永久终止 Parser 对象；释放前尚未完成的请求不会恢复，后续请求会开始新的解析会话。
_Avoid_: Parser 永久销毁、恢复旧请求
